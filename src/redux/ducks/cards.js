const initialState = {};

const Cards = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

//тут будут экшн креэйторы

//тут будут санки

export default Cards;
